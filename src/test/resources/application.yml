spring:
  datasource:
    url: jdbc:h2:mem:recebimento;MODE=Oracle;DB_CLOSE_DELAY=-1
    username: sa
    password: 
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none
    
flyway:
  validate-on-migrate: false