# Define a configuração para rodar o recebimento
version: '2'
services:
  recebimento:
    extends:
      file: compose/shared/common.yml
      service: recebimento
    depends_on:
     - rabbit
     - discovery