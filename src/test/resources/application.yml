spring:
  datasource:
    url: jdbc:h2:mem:recebimento;MODE=Oracle;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none
      use-new-id-generator-mappings: true
    
flyway:
  validate-on-migrate: false
  table: SCHEMA_VERSION